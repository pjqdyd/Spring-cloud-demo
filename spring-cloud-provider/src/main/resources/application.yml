USER: admin
PASSWORD: 123456

server:
  port: 8000

spring:
  application:
    name: provider-server

eureka:
  client:
    service-url:
      defaultZone: http://${USER}:${PASSWORD}@localhost:10000/eureka/