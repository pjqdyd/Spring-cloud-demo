USER: admin
PASSWORD: 123456

server:
  port: 1000

spring:
  application:
    name: api-gateway-zuul
  #当前应用的security配置
  security:
    user:
      name: admin
      password: 123456

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://${USER}:${PASSWORD}@localhost:10000/eureka/
